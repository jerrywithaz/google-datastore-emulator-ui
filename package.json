{
  "name": "google-datastore-emulator-ui",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve:client": "serve -s client/build",
    "dev:client": "cd client && npm run start",
    "dev:server": "nodemon server/scripts/dev.ts",
    "clean": "rm -rf lib",
    "build": "npm run clean && npm run build:scripts && npm run build:client && npm run build:server",
    "build:scripts": "tsc -P tsconfig.json",
    "build:client": "cd client && npm run build && mv build ../lib/client",
    "build:server": "cd server && npm run build && mv dist ../lib/server"
  },
  "author": "",
  "license": "ISC",
  "bin": {
    "google-datastore-emulator-ui": "./dist/start.js"
  },
  "devDependencies": {
    "@types/node": "^16.10.2",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "commander": "^8.2.0",
    "serve": "^12.0.1"
  }
}
